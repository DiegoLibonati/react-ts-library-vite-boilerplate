services:
  boilerplate-app:
    build:
      context: ./
      dockerfile: Dockerfile.development
    restart: unless-stopped
    volumes:
      - .:/home/app
      - /home/app/node_modules
    ports:
      - 6006:6006
    networks:
      - boilerplate-net
    stdin_open: true
    tty: true

networks:
  boilerplate-net: